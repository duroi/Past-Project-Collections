#include <string>
#include <vector>

using namespace std;

#include "digraphMatrix.h"


void DigraphMatrix(const string& graph) {

    ifstream inFile(graph);

    inFile >> this->n;

    inFile >> this->edgeCount
}
